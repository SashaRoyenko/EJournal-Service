spring:
  datasource:
    driverClassName: org.h2.Driver
    url: jdbc:h2:mem:myDb;DB_CLOSE_DELAY=-1;MODE=MySQL
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: create
    database: h2
  h2:
    console:
      enabled: true