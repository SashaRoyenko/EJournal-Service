server:
  port: 9090
  servlet:
    context-path: /e-journal

# spring
spring:
  application:
    name: '@project.artifactId@'
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/e-journal?serverTimezone=UTC
    username: root
    password: root
  jpa:
    hibernate.ddl-auto: update
    generate-ddl: true
    show-sql: true

